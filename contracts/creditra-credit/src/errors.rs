//! Contract error definitions for the CosmWasm creditra-credit package.
//!
//! Re-exports [`crate::error::ContractError`] so callers can use either
//! `crate::error` or `crate::errors` consistently with the Soroban package
//! naming in issue templates.

pub use crate::error::ContractError;
