[package]
name = "creditra-lifecycle"
version = "0.1.0"
edition = "2021"
description = "Creditra lifecycle v7 error stability tests"
license = "MIT"
keywords = ["soroban", "stellar", "lifecycle", "credit", "smart-contract"]
categories = ["cryptography::cryptocurrencies", "finance", "no-std"]
readme = "../../README.md"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
creditra-credit = { path = "../credit" }

[[test]]
name = "err_stab"
path = "tests/err_stab.rs"

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
creditra-credit = { path = "../credit", features = [] }
